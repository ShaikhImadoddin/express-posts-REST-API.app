<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Post</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen">
    <!-- Navbar -->
    <nav class="bg-white shadow-md p-4 mb-6">
        <div class="max-w-5xl mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold text-red-700">Mini Quora</h1>
            <a href="/posts" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md shadow">Back</a>
        </div>
    </nav>

    <!-- Page Heading -->
    <div class="text-center mb-6">
        <h2 class="text-xl font-bold text-gray-800 bg-white px-4 py-2 rounded-md shadow inline-block">
            Create a New Post
        </h2>
    </div>

   <!-- Form Card -->
<div class="flex justify-center">
  <div class="bg-white rounded-xl shadow-lg p-8 w-full max-w-md">
    <form action="/posts" method="POST" class="space-y-6">
      
      <!-- Username -->
      <div>
        <label for="username" class="block text-sm font-semibold text-gray-700 mb-1">Username</label>
        <input type="text" id="username" name="username" 
               class="mt-1 block w-full rounded-lg border border-gray-300 px-4 py-2 text-gray-700 
                      focus:border-red-500 focus:ring-2 focus:ring-red-300 focus:outline-none transition" 
               placeholder="Enter your name" required>
      </div>

      <!-- Content -->
      <div>
        <label for="content" class="block text-sm font-semibold text-gray-700 mb-1">Content</label>
        <textarea id="content" name="content" rows="5" 
                  class="mt-1 block w-full rounded-lg border border-gray-300 px-4 py-3 text-gray-700 
                         focus:border-red-500 focus:ring-2 focus:ring-red-300 focus:outline-none transition resize-none" 
                  placeholder="What's on your mind?" required></textarea>
      </div>

      <!-- Submit -->
      <div class="flex justify-center">
        <button type="submit" 
                class="bg-red-600 hover:bg-red-700 text-white px-8 py-2.5 rounded-lg shadow-md font-medium transition">
          Create Post
        </button>
      </div>

    </form>
  </div>
</div>
    
</body>
</html>